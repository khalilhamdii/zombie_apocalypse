import "babel-polyfill";
import HighScoreApi from "../Objects/HighScoreApi";

test("Expect AddNewScore to send the score to the API", () => {
  // HighScoreApi.getTopFive();
});

test("Expect getTopFive to return the objects with top 5 scores", () => {});

test("Expect getListOfScores to get a list of scores from the API", () => {});
